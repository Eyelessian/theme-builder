.default_rules:
  rules:

build-job:
  stage: .pre
  script:
    - npm ci
    - npm run build --if-present

test-job:
  stage: test
  script:
    - npm test

docker-job:
  stage: build
  script:
    - docker build .